spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: admin
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081